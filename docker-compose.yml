name: probe-shield

services:
  probe-shield:
    build: .
    image: probe-shield:local
    container_name: probe-shield
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${PROBE_SHIELD_HTTP_PORT:-3000}:${PROBE_SHIELD_HTTP_PORT:-3000}"
